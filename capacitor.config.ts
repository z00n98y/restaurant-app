import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'restaurant-56a43',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
